definition_version: 1
native_app:
   name: mcd_app
   artifacts:
      - src: app/*
        dest: ./
      - service/mcd_app_spec.yaml
   package:
      name: mcd_app_pkg
      role: mcd_repo_role
   application:
      name: mcd_app
      debug: false
      role: mcd_repo_role
